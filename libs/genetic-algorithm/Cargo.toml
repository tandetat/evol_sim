[package]
name = "genetic-algorithm"
version = "0.1.0"
authors = ["Diogo Tandeta Tartarotti"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rand = "0.8"

[dev-dependencies]
rand_chacha = "0.3"